import { Link, Outlet } from "react-router-dom";

// TransactionOperations component
function TransactionOperations() {
    return (
      <div>
        <Outlet />
      </div>
    );
  }

  export default TransactionOperations;